# Backlog

## Near Term

- add hover and click hit-testing on the canvas ladder
- animate print lifetime and drift inside the prints lane
- refine compressed spread rendering for buckets that contain both bid and ask
- add manual recenter and scroll controls on top of the current sticky viewport
- add tests for ApeX Omni depth sync and trade deduplication
- add unit tests for Binance snapshot/delta synchronization
- add reconnect backoff and metrics overlay
- add rolling or session-based cluster reset modes
- expose host callbacks for price click / row hover / subscription state

## Medium Term

- add adapter interface for more venues
- support manual center price and scroll lock
- add own orders, position row, and PnL markers
- add imbalance and delta highlighting modes

## Long Term

- separate visual theme package from market-data engine
- add order-entry mode behind explicit feature flag
- support multi-symbol coordination with a host screener
